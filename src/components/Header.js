import { useEffect } from "react";
import NavBar from "./Navbar";


function Header() {

    useEffect( () => {
        
    },[])

    return(
        <header className="header">
            <NavBar />
        </header>
    );
}

export default Header;